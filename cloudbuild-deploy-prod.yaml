steps:
  # build the container image
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/$PROJECT_ID/paas-app", "."]
    # push the container image
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/paas-app"]
    # deploy to Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run",
        "deploy",
        "paas-app",
        "--image",
        "gcr.io/$PROJECT_ID/paas-app",
        "--region",
        "europe-west4",
        "--platform",
        "managed",
        "--allow-unauthenticated",
        "--description",
        "$TRIGGER_NAME:$BRANCH_NAME-$SHORT_SHA",
      ]
options:
  logging: CLOUD_LOGGING_ONLY
